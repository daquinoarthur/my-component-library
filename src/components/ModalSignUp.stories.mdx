import { Meta, Story, Preview } from '@storybook/addon-docs/blocks'
import { withKnobs, boolean } from '@storybook/addon-knobs'

import { ModalSignUp } from './ModalSignUp'
import { GlobalStyle } from '../utils'

<Meta title="Design System | Modals" component={ModalSignup} />

# Modals

Modals are dialogs which overlay content to convey.

## Usage

Use modals to call the user to action, confirm or complete a task

## ModalSignUp

The ModalSignUp is used to prompt the user to create an account

<Preview withToolbar>
  <Story name="signUpModal">
    <ModalSignUp showModal={boolean("Show Modal", true)} />
  </Story>
</Preview>
